[← Powrót do spisu komponentów](../../components_storybook.md)

# PricingBox

Source: `src/components/molecules/PricingBox/PricingBox.tsx`

Opis:
- Kafelek cennika używany w sekcji `Pricing`.

Props (sprawdź implementację dla szczegółowych typów):
- `name: string`, `price: string`, `onClick: () => void`

Użycie:
```tsx
import { PricingBox } from 'src/components/molecules/PricingBox/PricingBox';
<PricingBox name="Pełna wersja" price="0" onClick={() => {}} />
```
